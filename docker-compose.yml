services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    image: backend

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    image: frontend
    ports:
      - "3000:80"
